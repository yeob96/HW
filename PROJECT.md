# 출퇴근 기반 부동산 검색 서비스 — 프로젝트 스펙

## 1. 프로젝트 개요

기존 부동산 플랫폼(네이버·직방·다방)은 "지역 → 매물" 순서로 탐색하지만,
이 서비스는 "직장 위치 → 출퇴근 가능 시간 → 예산" 순서로 입력받아
**살 수 있는 지역을 자동으로 계산**해주는 서비스다.

실매물 데이터는 다루지 않고, **국토교통부 실거래가 공개 데이터**를 기반으로
직장 기준 출퇴근 가능 지역과 가격대를 보여주는 것을 목표로 한다.

## 2. 핵심 사용자 흐름

1. 사용자가 직장 위치를 입력 (주소 또는 지도에서 선택)
2. 출퇴근 조건 입력 — 이동수단(대중교통/자차), 최대 소요시간
3. 예산 조건 입력 — 거래유형(매매/전세/월세), 가격대
4. 결과: 조건에 맞는 지역을 지도에 표시 + 지역별 실거래가 요약
5. 지역 클릭 시 상세 화면: 출퇴근 경로, 소요시간, 최근 실거래가 목록, 가격 추이

## 3. 기술 스택

| 영역 | 기술 |
|---|---|
| 프론트엔드 | React + Vite |
| 백엔드 | Spring Boot |
| DB | MySQL |
| 지도/좌표 | 네이버 지도 API (NAVER Cloud Platform Maps), 네이버 Geocoding API |
| 자차 소요시간 | 네이버 클라우드 플랫폼 Direction 5/15 API |
| 대중교통 소요시간 | ODsay Lab API |
| 부동산 데이터 | 국토교통부 실거래가 공개시스템 Open API (data.go.kr) |
| 시세 참고 | 한국부동산원 R-ONE 통계 (선택) |


## 4. 데이터 파이프라인

```
법정동 코드 목록
     ↓
국토교통부 실거래가 API 호출 (지역별/기간별 거래 내역 수집)
     ↓
지번/도로명 주소 → 네이버 Geocoding API로 지오코딩 (좌표 획득)
     ↓
PostgreSQL 저장 (좌표 + 가격 + 거래일 + 면적 등)
     ↓
프론트 요청 시: 직장 좌표 기준 네이버 Direction API/ODsay로 소요시간 계산 → 조건 필터링
```

## 5. DB 스키마 초안

- `transactions` (실거래가 원본 정제 데이터)
  - id, dong_code(법정동코드), address, lat, lng, deal_type(매매/전세/월세),
    price, deposit, monthly_rent, area, deal_date, apt_name
- `regions` (법정동 단위 집계용, 선택)
  - dong_code, region_name, avg_price, transaction_count, updated_at
- `commute_cache` (직장 좌표 ↔ 지역 소요시간 캐싱, API 호출 절감용)
  - workplace_lat, workplace_lng, dong_code, transit_minutes, car_minutes, cached_at

## 6. MVP 범위

**포함**
- 직장 위치 입력 + 지오코딩
- 출퇴근 시간/예산 조건 입력 폼
- 조건에 맞는 법정동 목록 계산 (소요시간 API + 실거래가 평균가 필터)
- 지도에 지역 마커 + 매물 수(거래 건수)/평균가 표시
- 지역 클릭 시 최근 실거래 내역 리스트 + 가격 추이 차트

**MVP 이후 (확장 여지로만 기재)**
- 공인중개사 직접 매물 등록 연동
- 플랫폼 공식 제휴를 통한 실매물 노출
- 사용자 관심 지역 저장/알림

## 7. 열린 결정 사항

- [ ] 프론트 상태관리 (Context API vs Zustand 등)
- [ ] 스타일링 방식
- [ ] 소요시간 API 호출 빈도 및 캐싱 전략 (쿼터 제한 대응)
- [ ] 실거래가 데이터 배치 수집 주기 (스케줄러 vs 온디맨드)
- [ ] 배포 환경
